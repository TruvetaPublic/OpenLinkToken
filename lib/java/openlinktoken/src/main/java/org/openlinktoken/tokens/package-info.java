/**
 * Provides token definitions, signature generation, tokenization, and token results.
 *
 * <p>{@link TokenGenerator} resolves configured attribute expressions from field-ID or
 * legacy class-keyed person data, then applies tokenization and configured transformations.
 * Optional inference providers can supply signatures for additional token identifiers.
 */
package org.openlinktoken.tokens;
